mode: arc analyse
rpts:
  invs0 : {path: "rpt/invs_rpts/axi4lscope.simple.gz", type: "invs"}
  invs1 : {path: "rpt/invs_rpts/axi4lscope.simple.gz", type: "invs"}
configs:
  output_dir: output
  analyse_tuples:
    - ["invs0", "invs1"]
  fanout_filter: x >= 1
  delay_filter: x >= 0
