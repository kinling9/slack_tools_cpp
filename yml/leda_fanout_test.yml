mode: fanout analyse
rpts:
  leda0 : {path: "rpt/leda_rpts/B005_allpath.rpt.gz", type: "leda"}
configs:
  output_dir: output
  analyse_tuples:
    - ["leda0"]
  fanout_filter: x >= 1
  slack_filter: x < 0
